// ── Supabase connection ──────────────────────────────────────────────────
// Paste your project's two PUBLIC values here. Find them in your Supabase
// dashboard: Project Settings → API.
//   • Project URL        → SUPABASE_URL
//   • Project API keys → "anon public" key → SUPABASE_ANON_KEY
//
// These are safe to commit to a public repo: the anon key only allows what your
// Row Level Security policies permit (each signed-in user can touch only their
// own garden row). See SUPABASE_SETUP.md for the one-time database setup.
//
// Leave the placeholders as-is and the app simply runs local-only (no sync).

export const SUPABASE_URL = "https://YOUR-PROJECT-ref.supabase.co";
export const SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";
